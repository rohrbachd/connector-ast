{
  "name": "@connector/control-plane",
  "version": "1.0.0",
  "description": "Control Plane implementation for the Lightweight Dataspace Connector",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "lint": "eslint . --ext .ts",
    "test": "vitest run",
    "dev": "fastify start -w -l info"
  },
  "dependencies": {},
  "devDependencies": {},
  "publishConfig": {
    "access": "public"
  }
}